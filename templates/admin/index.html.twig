{% extends 'base.html.twig' %}

{% block title %}Admin Page{% endblock %}

{% block body %}
    <div class="container my-5">
        <h2 class="text-center text-success mb-5 fw-bold">Admin Page</h2>
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card shadow-lg border-0 rounded-4 mb-5">
                    <div class="card-header bg-success text-white rounded-top-4 text-center py-3">
                        <h4 class="mb-0">Add New Product</h4>
                    </div>
                    <div class="card-body p-5 bg-white">
                        {{ include('admin/productRegisterForm.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
        {{ include('admin/productTable.html.twig') }}
    </div>
{% endblock %}
